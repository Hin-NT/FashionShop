package com.example.FashionShop.enums;

public enum DiscountStatus {
    ACTIVE,    // The discount is currently ongoing.
    UPCOMING,  // The discount is planned but has not started yet.
    EXPIRED;   // The discount has expired.
}
